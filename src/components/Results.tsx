import {TResult} from "@/libs/types";

type TResultsProps = {
    results: TResult[];
};

function Results({ results }: TResultsProps ) {
    return (
        <section>

        </section>
    )
};
export default Results;